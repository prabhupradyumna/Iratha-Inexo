<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
     <record id="report_partner_ledger" model="ir.actions.report">
        <field name="name">Partner Ledger</field>
        <field name="model">partner.ledger.wizard</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">partner_ledger.report_partner_ledger_html</field>
        <field name="report_file">partner_ledger.report_partner_ledger_html</field>
        <field name="print_report_name">'Ledger - %s' % (object.partner_id.name)</field>
    </record>

</odoo>